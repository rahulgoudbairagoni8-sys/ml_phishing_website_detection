Open terminal or command prompt, navigate to the folder where the files are stored, and run the following command to install the required packages.

"pip install -r requirements.txt"

After the installation is complete, run this command to start the application. The user interface will automatically open in default web browser.

"streamlit run app.py"